# Generate Test Data for Spider Plot Module

Generate Test Data for Spider Plot Module

## Usage

``` r
generate_test_data(n_subjects = 10, seed = NULL)
```

## Arguments

- n_subjects:

  `[integer(1)]` Number of subjects to generate (default: 10)

- seed:

  `[integer(1) | NULL]` Random seed for reproducibility (default: NULL)
